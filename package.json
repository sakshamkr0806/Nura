{
  "name": "nura",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently --names \"BACKEND,FRONTEND,INFO\" --prefix-colors \"blue,green,yellow\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" \"sleep 6 && echo '\\n✅ All servers running:\\n   🌐 Frontend: http://localhost:5173\\n   🚀 Backend:  http://localhost:3000\\n'\"",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.1",
    "husky": "^8.0.0",
    "lint-staged": "^16.4.0"
  },
  "lint-staged": {
    "frontend/src/**/*.{ts,tsx}": [
      "npm --prefix frontend run lint -- --fix",
      "npm --prefix frontend run format"
    ],
    "backend/src/**/*.ts": [
      "npm --prefix backend run lint -- --fix",
      "npm --prefix backend run format"
    ]
  }
}
